<div class="container mt-5 ">
    <div class="row">
        <div class="col-md-7 ">
            <img src={{course.image}} class="rounded-4 w-100" alt={{course.name}}>
        </div>
        <div class="col-md-5">
            <h3>{{course.name}}</h3>
        </div>
    </div>
</div>